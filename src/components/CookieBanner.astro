---
---
<style>
  #cookie-banner {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: #222;
    color: #fff;
    padding: 0.5rem 0.7rem;
    z-index: 1000;
    display: none;
    justify-content: space-between;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.97rem;
    flex-wrap: wrap;
    box-shadow: 0 -2px 12px #0003;
  }
  #cookie-banner span {
    flex: 1 1 120px;
    min-width: 0;
    word-break: break-word;
  }
  #cookie-accept-btn {
    background: #4fc3f7;
    color: #222;
    padding: 0.4rem 0.8rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.97rem;
    font-weight: 600;
    margin-right: 1.1rem;
    flex-shrink: 0;
    margin-left: -0.2rem;
  }
  @media (max-width: 600px) {
    #cookie-banner {
      flex-direction: row;
      align-items: center;
      text-align: left;
      font-size: 0.96rem;
      padding: 0.5rem 0.2rem;
      gap: 0.2rem;
    }
    #cookie-accept-btn {
      width: auto;
      margin-left: 0.5rem;
      margin-top: 0;
      font-size: 0.97rem;
    }
  }
</style>
<div id="cookie-banner">
  <span>
    Diese Website verwendet Cookies, um Ihnen das beste Erlebnis zu bieten.
    <a href="/datenschutz" style="color:#4fc3f7;text-decoration:underline;">Mehr erfahren</a>
  </span>
  <button id="cookie-accept-btn">
    Akzeptieren
  </button>
</div>

<script is:inline>
  // Zeige das Banner nur, wenn noch nicht akzeptiert
  if (typeof window !== "undefined") {
    if (!localStorage.getItem('cookiesAccepted')) {
      document.getElementById('cookie-banner').style.display = 'flex';
    }
    document.getElementById('cookie-accept-btn').onclick = function() {
      localStorage.setItem('cookiesAccepted', 'true');
      document.getElementById('cookie-banner').style.display = 'none';
    }
  }
</script> 